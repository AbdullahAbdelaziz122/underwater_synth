# Hierarchical Underwater Acoustic Classification Pipeline
# Requirements
#
# Jetson Nano (JetPack 4.6) — install PyTorch and torchaudio via the
# NVIDIA-provided wheels (see README), then install the rest with pip.
#
# Standard install (non-Jetson):
#   pip install -r requirements.txt

# Core
torch>=1.9.0
torchaudio>=0.9.0

# Utilities (all in stdlib on Python 3.6+, listed for clarity)
# pathlib  — stdlib
# logging  — stdlib
# argparse — stdlib
# json     — stdlib
# typing   — stdlib
# dataclasses — stdlib (Python 3.7+; backport available for 3.6)
dataclasses; python_version < "3.7"
